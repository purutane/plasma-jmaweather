# 変更履歴

`metadata.json` の `Version` ごとにまとめている。日付は各版が入った日。
利用者から見て何が変わるかを先に書き、理由が要るものだけ添える。

## 1.2.3 — 2026-08-16

### 修正

- パネルの表示を「アイコンのみ」にしていても、読み込み中と取得失敗中だけ `–` が
  出ていたのを直した
- 応答が返らない相手に当たると更新ボタンと再判定ボタンが無効のまま固まることがあったのを
  直した。20 秒で打ち切る。切断や DNS 失敗ではなく、繋がったまま黙っている場合に起きる
- 展開表示の回転インジケータが、概況や警報をまだ取っている途中で止まらないようにした

### 内部

コードの整理だけで、表示や設定は変えていない。

- 通信を `HttpRequest.qml` に集約（見張りの Timer 付き）
- 自動判定と手動指定のどちらを使うかの判断を `Locate.effectiveArea()` に一本化。
  パネルと設定画面が別々に持っていた
- 展開表示の中身をセクションごとの部品に分割（`*Section.qml`・`ForecastHeader.qml`）。
  `main.qml` は 783 行から 501 行に
- 生成物を表（`AreasData.js`・`TelopsData.js`・`WarnCodesData.js`）と手書きのロジックに分けた。
  表の中身は分割前と同じ
- `Forecast.js` の重複した小物を畳み、使われていない `levelLabel()` と週間予報の
  `reliability` を落とした

## 1.2.2 — 2026-08-14

### 修正

- 今日の最高気温と最低気温に同じ値が並ぶこと（`29°/29°`）があったのを直した。
  5時・11時発表では今日の気温の枠が `[09時, 00時]` の逆順で届き、後ろの00時には
  最高気温と同じ値が入っている。時刻ではなく並び順で振り分ける
- 空いた今日の最低気温は、前の日の配信で「先の日の最低」として通過した値を設定に控えて
  当てはめる（気象庁は今日の朝の最低を前の日までしか配信しない）。通信は増えない
- 142 地域中 78 で別の場所の気温を出していたのを直した。気温の観測所を週間予報から
  逆算せず、`forecast_area.json` 由来の対応表（`Areas.stations`）から引く。
  千葉県北西部が銚子の気温になっていた例がある

### 内部

- `Areas.js` に地域ごとの気温観測所 `stations` を生成して持たせた（`stationsOf()`）。
  21 地域は観測所を複数持ち、先頭が配信に載っていない区域が 1 つある（愛媛県中予）ので
  候補は順に試す

## 1.2.1 — 2026-08-13

### 修正

- 警報・注意報が発表中でも何も出ないことがあったのを直した。読んでいた
  `bosai/warning/data/warning/` が 2026-05-28 で更新を止めており、気象庁のページ自身が
  読んでいる `data/r8/` へ移した。千葉県北西部に大雨特別警報（レベル5）が出ている最中でも
  静かなままだった
- 新しい配信は電文ごとの配列で、大雨・土砂災害・雷などが別々の要素で届く。全部を混ぜてから
  まとめる（1 要素目だけ見ると同じ地域の警報を取りこぼす）
- 発表時刻は電文ごとに違うので一番新しいものを、見出しは一番重い警報を伝えているものを使う
- `status` の「警報から注意報」「危険警報から警報」は解除ではなく格下げの発表中なので残す

## 1.2.0 — 2026-08-13

### 追加

- 選んでいる地域に発表中の警報・注意報を表示する。パネルに印が付くのは警報以上のときだけで、
  注意報は展開表示にとどめる（雷・濃霧の注意報は全国のどこかで年中出ている）
- 発表が無いときと取得に失敗したときを書き分ける。空白のままだと警報を見ているのかどうかが
  分からない
- 警報・注意報の表示を設定で切れるようにした
- コード表 `WarnCodes.js` を生成物として追加。レベルは 20 注意報 / 30 警報 / 40 危険警報 /
  50 特別警報で、数値の大小がそのまま深刻さの順になる。表に無いコードは捨てずに警報として扱い、
  コード番号のまま見せる（黙って落とすと、コードが増えた日に特別警報を出し損ねる）

## 1.1.1 — 2026-08-13

### 修正

- 展開表示の中身がポップアップの縁に張り付いていたのを直した。ヘッダーの高さが固定されて
  判定失敗の行がはみ出す件と、概況が空のとき下端の余白ごと消える件も併せて直した

## 1.1 — 2026-08-13

### 追加

- 現在地を IP から自動判定するようにし、既定をそれにした。外れたときのために
  「予報区」「地域」の手動指定は残している（日本の回線は契約者の登録地に丸められることがあり、
  同じ回線でもサービスによって県が変わる）
- 判定した区域は設定に保存し、1 日 1 回しか取り直さない。座標が読めないとき・国外のとき・
  最近傍が 100km を超えるときは手動側の指定へ落とし、展開表示にその旨を出す
- 座標を得るところだけ気象庁の外（`get.geojs.io` / `ipwho.is`、どちらもキー不要・HTTPS）に
  問い合わせる
- 市区町村の代表点 `Geo.js` を生成物として追加。飛び地を抱える市区町村は外接矩形の中心が
  海に落ちるため、有人島の位置を名指しで補正している（小笠原村は対角 2000km 近い）

### 変更

- 1.0 から更新した人を勝手に自動判定へ動かさないよう、既定値と違う地域を選んでいた設定は
  初回起動時に手動へ寄せる
- 設定ページの根を `KCM.SimpleKCM` にした。`FormLayout` を直に置くと先頭行がタイトルバーに
  貼り付き、スクロールも効かない

## 1.0 — 2026-08-12

### 追加

- 気象庁の府県天気予報・週間天気予報の JSON を直接読み、パネルに天気アイコンと気温を
  常駐表示する。展開すると天気文、これからの降水確率、週間予報 7 日分、府県概況を出す。
  全国 58 予報区 142 地域に対応
- 気象庁の JSON は区域のくくりが系列ごとに違うため、全国分の実データで確認して対応させた。
  週間予報は短期予報より粗く、短期側に無いコードを持つ週間区域を受け皿として寄せる。
  十勝と奄美は `area.json` のコードでは配信されていないので別コードへ読み替える

### 変更

- プラグイン ID を `org.kde.*` から `io.github.purutane.jmaweather` へ移した。
  `org.kde.*` は KDE 本体のための名前空間で、第三者のウィジェットが名乗るものではない。
  パネルへ追加する前なので既存設定の移行は不要

### 内部

- 解析ロジックのテストとフィクスチャ、`tests/run.js`（`qmllint` 込み）を追加
- `CLAUDE.md` を追加。入れ直さないと表示が変わらない件と、機能追加の進め方を固定した
